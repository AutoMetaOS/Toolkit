{
  "name": "amoskit",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "deploy": "npx gh-pages -d build -t true",
    "start": "php -q -S localhost:3000"
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "@sveltejs/adapter-static": "^1.0.0-next.17",
    "sass": "^1.38.1",
    "svelte-preprocess": "^4.8.0"
  }
}